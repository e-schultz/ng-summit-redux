export default class PendingOrdersController {
  constructor() {
    // This actually isn't needed 
    // look at completed-orders for an example
    // If you had some 'component' logic that you wanted to do
    // before displaying or dispatching callbacks, then it could
    // go here.
    // If your component is only rendering data, you can go without
    // the controller
  }
};
